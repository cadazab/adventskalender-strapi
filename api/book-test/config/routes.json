{
  "routes": [
    {
      "method": "GET",
      "path": "/book-tests",
      "handler": "book-test.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/book-tests/count",
      "handler": "book-test.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/book-tests/:id",
      "handler": "book-test.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/book-tests",
      "handler": "book-test.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/book-tests/:id",
      "handler": "book-test.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/book-tests/:id",
      "handler": "book-test.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
